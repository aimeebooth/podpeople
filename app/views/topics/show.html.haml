%h2 #{topic.name}

%h3 Description
%p= topic.description
    
%p= link_to 'Edit', edit_episode_topic_path(topic.episode, topic)
%p= link_to 'Delete', episode_topic_path(topic.episode, topic), method: :delete, confirm: "Are you sure you want to ba-lete this?"

%p= link_to "Back to #{topic.episode.title}", show_episode_path(topic.episode.show, topic.episode)