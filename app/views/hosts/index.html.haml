%h2 Hosts Index

%table
  %tr
    %th Name
    %th
  - hosts.each do |host|
    %tr
      %td= link_to host.name, [host.show, host]
      %td= link_to 'Edit', edit_show_host_path(host.show, host)
      %td= link_to "Delete", [host.show, host], method: :delete, confirm: "Are you sure you want to ba-lete this?"

%p= link_to 'Add Host', new_show_host_path(show)
